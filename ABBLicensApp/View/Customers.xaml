<Page
    x:Class="ABBLicensApp.View.Customers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ABBLicensApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:ABBLicensApp.ViewModel"
    xmlns:kocal="using:ABBLicensApp.Viewmodel"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style x:Key="CustomerBtn" TargetType="Button">
            <Setter Property="Height" Value="100" />
            <Setter Property="Width" Value="200" />
            <Setter Property="Background" Value="LightGray" />
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="20"/>
        </Style>
    </Page.Resources>

    <Page.DataContext>
        <kocal:CustomersViewModel/>
    </Page.DataContext>
    
    <ListView ItemsSource="Customers">
        <ListView.ItemTemplate>
            <DataTemplate>
                <Button Style = "{StaticResource CustomerBtn}" Content="{Binding Email}"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</Page>
